# Copy this file to .env and fill in your values.
# .env is gitignored and will not be committed.

# HuggingFace token (required for downloading gated models like pocket-tts voice cloning)
# Get yours at https://huggingface.co/settings/tokens
HF_TOKEN=

# OVA configuration (these can also be set as shell env vars)
# OVA_PROFILE=default
# OVA_TTS_ENGINE=kokoro            # kokoro | pocket_tts
# OVA_LLM_BACKEND=ollama           # ollama | koboldcpp
# OVA_LLM_MODEL=                   # model name (auto-detected per backend if empty)
# OVA_KOBOLDCPP_URL=http://localhost:5001
# OVA_POCKET_TTS_VOICE=alba        # alba | marius | javert | jean | fantine | cosette | eponine | azelma
